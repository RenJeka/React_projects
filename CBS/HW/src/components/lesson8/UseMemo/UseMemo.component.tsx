import React from 'react';
import classes from './UseMemo.component.module.scss'

const UseMemoComponent = () => {
    return (
        <div className={classes.wrapper}>
            Use memo component
        </div>
    );
};

export default UseMemoComponent;
